using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectHolder : MonoBehaviour
{
    // This identifies the scene object that will be a parent of all instantiated tanks, vfx, sfx, etc.
    // This way they all get destroyed when the scene is unloaded! YAY!
}
